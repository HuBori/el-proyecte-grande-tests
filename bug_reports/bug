1.) should filter all employees not just the ones on current page
2.) logged in - after refresh visibly logged out but actually logged in
3.) shouldn't store password in input value attribute
4.) after logout shouldn't fill login fields automatically
5.) employee edits should be saved
6.) after filter transactions when filter deleted the page dies

--print screens when "egyértelmű"

// there should be an improvement file also. :)